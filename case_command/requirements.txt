# Core Case Command has NO required third-party dependencies.
# The web UI needs Jinja2:
jinja2>=3.0

# Optional extraction backends. Each is imported through optional_import() and
# its absence is reported by `case-command health`, never raised as an error.
# pypdf>=4.0            # PDF native text
# python-docx>=1.0      # DOCX (a stdlib ZIP+XML fallback exists)
# openpyxl>=3.1         # XLSX
# pillow>=10.0          # image metadata, required by pytesseract
# pytesseract>=0.3      # OCR of scanned filings (also needs the tesseract binary)
# extract-msg>=0.48     # Outlook .msg
# striprtf>=0.0.26      # RTF (a regex fallback exists)
